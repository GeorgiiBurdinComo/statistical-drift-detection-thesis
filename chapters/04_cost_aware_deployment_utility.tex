\chapter{Cost-Aware Deployment Decisions}
\label{ch:cost-deployment-utility}

\noindent Chapter~\ref{ch:drift} establishes a directional monitoring signal on the fixed panel \(\mathcal{D}_n\). When that signal and its diagnostics support escalation, the next step is a broader rerun on the full benchmark \(\mathcal{D}_N\) of 1199 labelled posts. That rerun serves a different role from the sentinel panel: it places candidate model--prompt configurations on a common evaluation basis and supplies the false-positive counts, false-negative counts, and observed inference charges needed for deployment comparison. This chapter turns those benchmark outcomes into a model-selection rule under declared task-specific losses~\cite{elkan2001cost,turney2000cost}.

\section{From panel signal to benchmark comparison}

Let
\(\mathcal{M}_{\mathrm{cand}}=\{m_1,\ldots,m_J\}\) be the candidate set of model--prompt configurations at evaluation time \(t\).

Let \(\mathcal{B}_t\subseteq\mathcal{D}_N\) be the benchmark items with a valid parsed prediction and usage record for every candidate, and write \(n_t=|\mathcal{B}_t|>0\). Using the same items for every candidate keeps the ranking attributable to model differences rather than to item composition.

\paragraph{Step 1: confusion-matrix decomposition.}
On the common rerun set \(\mathcal{B}_t\), candidate \(m\) at evaluation time \(t\) produces confusion counts \(\mathrm{TP}_t(m)\), \(\mathrm{FP}_t(m)\), \(\mathrm{TN}_t(m)\), and \(\mathrm{FN}_t(m)\) satisfying
\[
    \mathrm{TP}_t(m)
    +\mathrm{FP}_t(m)
    +\mathrm{TN}_t(m)
    +\mathrm{FN}_t(m)
    =
    n_t.
\]
Let \(c_t(m)\) denote the mean inference charge in USD per request on \(\mathcal{B}_t\). This quantity can differ across models, so the comparison uses the average observed inference cost for candidate \(m\) over the requests in \(\mathcal{B}_t\). The scenario penalties \(C_{\mathrm{FP}}\ge0\) and \(C_{\mathrm{FN}}\ge0\) assign a business cost in USD to one false positive and one false negative, respectively.

\paragraph{Step 2: total batch cost.}
The total batch cost is the sum of inference cost and business-error cost:
\begin{description}
    \item[\(n_t\)] size of the common rerun set;
    \item[\(c_t(m)\)] mean inference charge for candidate \(m\);
    \item[\(\mathrm{FP}_t(m),\ \mathrm{FN}_t(m)\)] observed false-positive and false-negative counts;
    \item[\(C_{\mathrm{FP}},\ C_{\mathrm{FN}}\)] assigned costs of one false positive and one false negative.
\end{description}
\begin{equation}
    \operatorname{TotalCost}_t(m)
    =
    \underbrace{n_t c_t(m)}_{\text{inference}}
    +
    \underbrace{
        C_{\mathrm{FP}}\mathrm{FP}_t(m)
        +C_{\mathrm{FN}}\mathrm{FN}_t(m)
    }_{\text{business error cost}}.
    \label{eq:total-batch-cost}
\end{equation}

\paragraph{Step 3: per-request expected cost.}
Dividing by \(n_t\) gives the empirical per-request loss
\begin{equation}
L_t(m)
=
c_t(m)
+
\frac{
    C_{\mathrm{FP}}\mathrm{FP}_t(m)
    +C_{\mathrm{FN}}\mathrm{FN}_t(m)
}{n_t}.
\label{eq:expected-cost}
\end{equation}
The counts and charges are observed on \(\mathcal{B}_t\), whereas \(C_{\mathrm{FP}}\) and \(C_{\mathrm{FN}}\) are declared scenario inputs. Changing the penalties therefore changes the deployment objective.

\paragraph{Step 4: model selection rule under fixed costs.}
When \(C_{\mathrm{FP}}\) and \(C_{\mathrm{FN}}\) are declared, the preferred deployment candidate is
\begin{equation}
    m_t^\star
    \in
    \arg\min_{m\in\mathcal{M}_{\mathrm{cand}}}
    L_t(m).
    \label{eq:model-selection}
\end{equation}
The set-valued \(\arg\min\) allows ties and therefore leaves room for a declared secondary rule when two candidates have the same loss. Table~\ref{tab:chapter4-cost-summary} illustrates the rule at one declared setting, \(C_{\mathrm{FP}}=0.155\) USD and \(C_{\mathrm{FN}}=0.031\) USD.

\begin{table}[htbp]
    \centering
    \small
    \caption[Illustrative benchmark comparison under declared task losses]{Illustrative benchmark comparison on the common complete-case rerun set under \(C_{\mathrm{FP}}=0.155\) USD and \(C_{\mathrm{FN}}=0.031\) USD.}
    \label{tab:chapter4-cost-summary}
    \input{assets/metrics_export/canonical_cost_summary.tex}
\end{table}
\reproanchor{The common-case comparison in Table~\ref{tab:chapter4-cost-summary} is computed in \codepath{canonical_evidence.py} from the frozen Langfuse exports \codepath{streamlit_app/langfuse_traces.csv} and \codepath{streamlit_app/langfuse_scores.csv}. The frozen comparison slice is \codepath{notebooks/canonical_evidence_export/deployment_cost_may22.csv}; the thesis table asset is \thesispath{assets/metrics_export/canonical_cost_summary.tex}.}

At this operating point, \texttt{gpt-5-mini} minimises the loss at \(0.0037\) USD per request. It combines five false positives and 62 false negatives with a low mean inference charge. The cheaper \texttt{gpt-5-nano} remains close, but its additional false negatives raise the loss to \(0.0039\). The accuracy leader \texttt{gpt-5.2} still carries a much larger loss because higher inference cost and more false positives are expensive under the stated operational asymmetry. The comparison therefore selects the model for the declared objective, rather than the model with the highest accuracy or the lowest API price in isolation.

\section{Preference parameters and decision map}
\label{sec:cost-parameters-uncertainty}

Two parameters separate the relative error trade-off from the overall severity of business errors:
\begin{equation}
    w
    =
    \frac{C_{\mathrm{FN}}}{C_{\mathrm{FP}}},
    \qquad
    \lambda
    =
    C_{\mathrm{FP}}+C_{\mathrm{FN}}.
    \label{eq:R-lambda-definition}
\end{equation}
Here \(w\) measures how costly a false negative is relative to a false positive, while \(\lambda\) measures how much business errors matter relative to inference price. The original penalties can be recovered from
\[
    C_{\mathrm{FP}}=\frac{\lambda}{1+w},
    \qquad
    C_{\mathrm{FN}}=\frac{\lambda w}{1+w}.
\]
Substituting these expressions into Equation~\eqref{eq:expected-cost} gives
\begin{align}
L_t(m;w,\lambda)
&=
c_t(m)
+
\frac{\lambda}{1+w}
\frac{
    \mathrm{FP}_t(m)+w\mathrm{FN}_t(m)
}{n_t}.
\label{eq:expected-cost-lambda-R}
\end{align}
This form exposes the decision logic directly: increasing \(\lambda\) gives more weight to business errors relative to API cost, and increasing \(w\) gives more weight to false negatives relative to false positives.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{assets/evidence/fig_canonical_cost_decision_map.pdf}
    \caption[Preferred candidate over task-loss assumptions]{Preferred candidate over relative false-negative cost \(w\) and total error severity \(\lambda\) on the common benchmark rerun. The cross marks the illustrative operating point from Table~\ref{tab:chapter4-cost-summary}. Region changes show where the deployment choice changes with the operational objective.}
    \label{fig:cost-decision-map-ch4}
\end{figure}
\reproanchor{The decision surface is generated by \codepath{make_canonical_figures.py} from \codepath{notebooks/canonical_evidence_export/deployment_cost_map_source.csv}; the operating point and winning model are recorded in \codepath{notebooks/canonical_evidence_export/canonical_summary.json}; the rendered thesis figure is \thesispath{assets/evidence/fig_canonical_cost_decision_map.pdf}.}

Figure~\ref{fig:cost-decision-map-ch4} turns Equation~\eqref{eq:expected-cost-lambda-R} into a decision surface. A point in the \((w,\lambda)\) plane specifies one operational objective; the region label at that point identifies the minimum-loss candidate. The cross marks the illustrative operating point, where \texttt{gpt-5-mini} is preferred. Crossing a region boundary changes the preferred deployment model because the weighted error savings no longer offset the competing model's API charge.

\section{From business requirements to model choice}
\label{sec:constraint-based-selection}

In practice, a deployment request starts with two questions. First, how costly should one business error be relative to one model call? This fixes \(\lambda\). Second, which error matters more, a false positive or a false negative? This fixes \(w\). Once those two inputs are declared, Equation~\eqref{eq:expected-cost-lambda-R}, Table~\ref{tab:chapter4-cost-summary}, and Figure~\ref{fig:cost-decision-map-ch4} identify the preferred candidate.

The same comparison is available interactively in the expected-cost dashboard view of Section~\ref{sec:metrics-dashboard}: a reviewer enters \(C_{\mathrm{FP}}\) and \(C_{\mathrm{FN}}\) (or their ratio), and the ranking highlights the current minimum-loss model under those scenario prices (Figure~\ref{fig:dashboard-expected-cost}).

\paragraph{Worked comparison: \texttt{gpt-5-mini} versus \texttt{gpt-5-nano}.}
On the shared rerun set, the loss formula is
\[
    L_t(m)
    =
    c_t(m)
    +
    \frac{
        C_{\mathrm{FP}}\mathrm{FP}_t(m)
        +C_{\mathrm{FN}}\mathrm{FN}_t(m)
    }{n_t},
\]
with \(n_t=1112\), \(C_{\mathrm{FP}}=0.155\), and \(C_{\mathrm{FN}}=0.031\). Substituting the observed values for \texttt{gpt-5-mini} gives
\[
\begin{aligned}
L_{\texttt{mini}}
&=
0.0012740
+
\frac{0.155\times5+0.031\times62}{1112}\\
&=
0.0012740
+
0.0006969
+
0.0017284\\
&=
0.0036994.
\end{aligned}
\]
For \texttt{gpt-5-nano}, the same substitution gives
\[
\begin{aligned}
L_{\texttt{nano}}
&=
0.0003258
+
\frac{0.155\times5+0.031\times105}{1112}\\
&=
0.0003258
+
0.0006969
+
0.0029272\\
&=
0.0039499.
\end{aligned}
\]
The same comparison can be written in the \((w,\lambda)\) parametrization with \(w=0.2\) and \(\lambda=0.186\):
\[
\begin{aligned}
L_{\texttt{mini}}
&=
0.0012740
+
\frac{0.186}{1+0.2}
\frac{5+0.2\times62}{1112}\\
&=
0.0012740
+
\frac{0.186}{1.2}
\frac{17.4}{1112}\\
&=
0.0036994,
\end{aligned}
\qquad
\begin{aligned}
L_{\texttt{nano}}
&=
0.0003258
+
\frac{0.186}{1+0.2}
\frac{5+0.2\times105}{1112}\\
&=
0.0003258
+
\frac{0.186}{1.2}
\frac{26}{1112}\\
&=
0.0039499.
\end{aligned}
\]
The two models have the same false-positive term, so the decision depends on the trade-off between extra inference cost and false-negative savings. Mini adds about \(0.00095\) USD per request in API cost, but it avoids 43 false negatives on 1112 evaluated posts, which reduces the false-negative term by about \(0.00120\) USD per request. The net effect is therefore about \(-0.00025\) USD per request, or about \(-0.025\) USD per 100 categorizations, so mini has the lower total loss.
\reproanchor{The worked substitutions for \(n_t=1112\), the per-model confusion counts, and the mini-versus-nano loss gap are checkable from \codepath{notebooks/canonical_evidence_export/deployment_cost_may22.csv} and \codepath{notebooks/canonical_evidence_export/canonical_summary.json}. The 100-categorization decomposition plotted next is rendered in \thesispath{assets/fig_mini_nano_tradeoff.tex}.}

\begin{figure}[htbp]
    \centering
    \input{assets/fig_mini_nano_tradeoff.tex}
    \caption[Mini-versus-nano expected-cost decomposition]{Expected-cost decomposition in USD cents per 100 categorizations for \texttt{gpt-5-mini} and \texttt{gpt-5-nano} at the declared operating point. Mini has the larger inference-cost component, both models have the same false-positive component, and nano has the larger false-negative component. The lower total bar for mini shows why it is preferred under the stated FP/FN costs.}
    \label{fig:mini-nano-tradeoff}
\end{figure}

Figure~\ref{fig:mini-nano-tradeoff} visualises the same calculation directly on a 100-categorization scale: mini is higher on inference cost, equal on false-positive cost, and lower on false-negative cost. If a business analyst can state those two preferences directly, the loss rule already gives the answer. If direct monetary calibration is unavailable, the same decision can be encoded through operational constraints on recall, budget, or precision. Table~\ref{tab:chapter4-cost-summary} therefore reports precision, recall, inference cost, and loss in one place, so the alternative decision rules below can be read against the same candidate roster.
\section{Scope of the decision}

The decision rule in Equation~\eqref{eq:model-selection} ranks the current candidate roster on the current common full-benchmark cases under declared penalties and observed inference charges. Small loss gaps therefore call for sensitivity reporting, close inspection of nearby decision boundaries, and a cautious deployment review rather than a claim of universal model superiority.
